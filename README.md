# atsh
a tiny shell. not suited for daily use
# install and compile
```bash
git clone https://github.com/chromalence/atsh &&
cd atsh && 
make
```
# install 
```bash
git clone https://github.com/chromalence/atsh &&
cd atsh
```
# compile 
```bash
make
```
